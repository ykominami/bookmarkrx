<%= form_with(model: proposer) do |form| %>
  <% if proposer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(proposer.errors.count, "error") %> prohibited this proposer from being saved:</h2>

      <ul>
        <% proposer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :session, style: "display: block" %>
    <%= form.text_field :session %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
