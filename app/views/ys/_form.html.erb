<%= form_with(model: y) do |form| %>
  <% if y.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(y.errors.count, "error") %> prohibited this y from being saved:</h2>

      <ul>
        <% y.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
