<%= form_with(model: choiceproposal) do |form| %>
  <% if choiceproposal.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(choiceproposal.errors.count, "error") %> prohibited this choiceproposal from being saved:</h2>

      <ul>
        <% choiceproposal.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_field :content %>
  </div>

  <div>
    <%= form.label :status, style: "display: block" %>
    <%= form.number_field :status %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
