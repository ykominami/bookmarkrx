<%= form_with(model: prevote) do |form| %>
  <% if prevote.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(prevote.errors.count, "error") %> prohibited this prevote from being saved:</h2>

      <ul>
        <% prevote.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
